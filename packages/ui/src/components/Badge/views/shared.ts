// Shared option lists: consumed by both the stories file's argTypes and the
// view files, so the axes stay in sync in one place.
export const intents = ['neutral', 'info', 'success', 'warning', 'danger'] as const
export const variants = ['solid', 'faded'] as const
export const sizes = ['sm', 'md', 'lg'] as const
